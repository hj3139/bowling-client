import { MainLayout } from 'layouts';
import { LoginForm } from 'components/Form';

const HomePage = () => {
  return (
    <MainLayout>
      <LoginForm />
    </MainLayout>
  );
};

export default HomePage;
